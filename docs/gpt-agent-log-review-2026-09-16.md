# GPT 系サブエージェントの運用記録(2026-09-16)

## この文書の役割

Claude Code のセッション記録から、GPT 系サブエージェントの運用を測った結果と、そこから直した内容を残す。
次に測るときの基準値として使う。
数え方をここに固定してあるので、同じ規則で数え直せば前回との差が読める。

## 測り方

記録の置き場は `%USERPROFILE%\.claude\projects` である。
この下にセッションごとの `.jsonl` があり、サブエージェントの記録は `<セッション>/subagents/` に入る。
1 行が 1 件の出来事で、`message.content` の中に `tool_use` と `tool_result` が並ぶ。

測る指標は 4 つである。

- **実起動**：`codex-agent.sh` を起動した Bash の呼び出しのうち、結果が記録に残ったものの件数。`codex-agent: result=` の値(`ok`、`rate-limited`、`unavailable`、`failed exit=<code>`)で分ける。
- **バックグラウンドへの移行**：結果の本文に「moved to the background」が出た起動の件数。
- **出力の退避**：結果の本文に「Output too large」が出た件数と、その最大のサイズ。
- **待つためだけの Bash**：Codex を呼んだサブエージェントの中で、`.output`、`sleep`、`until` を含む Bash の呼び出しの件数。Codex の起動そのものは含めない。

あわせて、定義ごとの内訳(親から見た起動、Codex を呼ばなかった件数、`git commit` を実行した件数)を出す。

数え方には 4 つの約束がある。

- 起動は Bash の呼び出し 1 件を 1 件と数える。同じ呼び出しが親とサブエージェントの両方の記録に現れることは無いので、時刻でまとめる重複除去は行わない。まとめると、同じ分に並行して起動した別々の実行が失われる。
- 依頼文はヒアドキュメントでコマンドに埋め込まれるため、照合の前にその本文を落とす。落とさないと、依頼文が話題にしている語(`git commit` など)を実行したものとして数える。
- `cat tools/codex-agent.sh` のようにスクリプトを読むだけのコマンドは起動と数えない。`bash` または `sh` がスクリプトを実行している形だけを数える。
- 委譲の内訳は、親の依頼文とサブエージェントの最初の受信文を全文で突き合わせて紐付ける。同じ依頼文の再送は 1 件として数える。

読めなかった場所と、解析できなかった行は、件数を出す。
測れなかったことと、実績が無いことを取り違えないためである。

出た値はすべて下限である。
理由は 2 つある。
Codex の実行はバックグラウンドへ移ることがあり、その出力が記録に残らない場合がある。
もう 1 つは、分類を終了コードと `result=` の行だけで行っている点である。
`codex exec` は依頼を果たせなくても自分の応答を返せば 0 で終わるため、「終了コード 0 だが報告はできなかった」という失敗はこの集計に現れない。

## 2026-09-16 時点の値

2026-09-09 から 2026-09-16 までを数えた結果である。
この期間の途中(2026-09-11 00:02)に GPT 優先の定義をユーザ側へ配り、認証ホームを 2 アカウントへ分けている。

| 指標 | 値 | 読み方 |
|---|---|---|
| 実起動(結果が記録に残ったもの) | 95 | 内訳は下の表 |
| バックグラウンドへ移された起動 | 36 | Codex の実行が Bash の上限を超えた回数 |
| 出力の退避 | 16 回(最大 950KB) | 標準出力が大きすぎてファイルへ移された回数 |
| 待つためだけの Bash | 216 | Codex を呼んだサブエージェントが、待つために使った実行 |

実起動の内訳は次のとおりである。

| `result=` | 件数 |
|---|---|
| (結果行なし) | 65 |
| `ok` | 17 |
| `failed exit=3` | 8 |
| `rate-limited` | 3 |
| `failed exit=1` | 1 |
| `unavailable` | 1 |

「結果行なし」が多いのは、標準出力が大きすぎてツール結果がファイルへ退避され、`result=` の行が記録に残らなかったためである。

委譲の内訳は次のとおりである。

| 定義 | 親から見た起動 | Codex 未呼出 | `git commit` を実行 |
|---|---|---|---|
| impl-hard | 57 | 42 | 22 |
| impl-standard | 54 | 1 | 16 |
| impl-light | 24 | 13 | 12 |
| codex-subagent | 5 | 1 | 1 |
| codex-review | 1 | 1 | 0 |

`impl-hard` の未呼出が多いのは、期間の前半でユーザ側の定義に `codex_model` が無く、Claude 側へ倒れていたためである。

## 見つかった問題と対応

| 問題 | 状態 |
|---|---|
| モデルの混雑で落ちると、フォールバックせず作業が止まる | #36 で対応。終了コード 75 の対象を GPT 側の事情全般へ広げ、`result=unavailable` で理由を分けた |
| 待ち方が定義に無く、サブエージェントが毎回自分で組み立てる | #36 で対応。5 定義に「実行が長引いたとき」の節を足した |
| 標準出力が大きすぎて報告を取り出せない | #36 で対応。経過をログファイルへ移し、標準出力を監査行、ログのパス、最終報告、結果の行に絞った |
| 転送ラッパーが Codex を呼ばずに自分で答える | #36 で対応。応答が有効である条件を定義と設計原則に書いた |
| 文書が書くスクリプトの探索順が実装と食い違う | #36 で対応 |
| 終了コード 3 が 4 つの状態を束ね、報告で区別できない | #36 で対応。理由行を報告の冒頭に置く形にした |
| リポジトリ側とユーザ側で effort がずれている | #36 で対応。出荷既定値を稼働値に揃えた |
| `impl-*` の禁止事項と委譲方針が依頼文で上書きされる | #34 で継続。優先順位と上書きの可否を決める |
| 作業ツリーの分離が手段で書かれ、守られていない | #35 で継続。不変条件で定め直す |
| `cross-review.js` が終了コード 75 を常に利用上限として記録する | #37 で継続。上流を直して再同期する |
| `codex-agent.sh` の分岐に自動テストが無い | #38 で継続 |
| 上限に当たった後も同じ枠の中で起動を繰り返す | 未着手。2026-09-10 以前だけで観測しており、アカウントを分けた後は再発していない |

## 次に測るときに見る点

対応が効いていれば、次の 3 つが下がる。

- **待つためだけの Bash。** 定義は `sleep` と `until` のループを禁じ、プロセスの一覧や出力ファイルの探索も禁じている。出力ファイルを読むこと自体は回数を制限していないので、この指標が 0 になるとは限らない。見るのは、`sleep` と `until` が消えたかどうかと、1 件の起動あたりの回数が数回に収まっているかである。減らないなら、定義がユーザ側へ配られていないか、セッションを再起動していない。
- **出力の退避と「結果行なし」。** 標準出力を絞ったので、どちらもほぼ 0 になるはずである。残るなら、依頼文が大きいか、ユーザ側のスクリプトが古い。
- **`failed exit=1`。** モデルの混雑がここに落ちなくなり、`unavailable` へ移るはずである。`unavailable` が現れず `failed exit=1` が残るなら、判定の語が足りていない。ログの `ERROR` 行を読んで語を足す。

下がらない指標も見る。

- **バックグラウンドへの移行。** これは Codex の実行時間そのものなので減らない。減らす手は effort を下げることであり、#31 に残っている。
- **委譲の内訳。** `impl-light` の未呼出が多いままなら、実装でない雑務が流れ込んでいる。#34 の判断材料になる。

測ったら、この文書の「2026-09-16 時点の値」の下に同じ形で追記する。
古い値を書き換えない。差が読めなくなるためである。
